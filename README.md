# PGVector AWS RDS Module

Features
- Creates RDS Postgres Instance
- Enables the `vector` extension

Included Modules/Provider
- `github.com/zeet-dev/terraform-aws-rds` (child module)
- `cyrilgdn/postgresql` (provider)
